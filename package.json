{
  "name": "clearcache",
  "version": "0.0.0",
  "private": true,
  "description": "ClearCache browser extension — dev/test tooling only. The shipped extension has zero runtime dependencies.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
